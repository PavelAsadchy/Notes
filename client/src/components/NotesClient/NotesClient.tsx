import { FC, ReactElement } from "react";

import './NotesClient.scss';

const NotesClient: FC = (): ReactElement => {
  return (
    <div className="notes">
      Notes
    </div>
  );
};

export default NotesClient;